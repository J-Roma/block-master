import {types} from '../types/Types'

const initialState = {
    movies=[],
    active=null
}

const movieReducers = (state) => {
    return (
        <div>
            
        </div>
    )
}

export default movieReducers
